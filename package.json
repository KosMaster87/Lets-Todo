{
  "name": "lets-todo-app",
  "version": "1.0.0",
  "description": "Lets Todo Frontend MPA/SPA Dev Server",
  "main": "dev-server.js",
  "type": "module",
  "scripts": {
    "dev": "concurrently -n SERVER,RELOAD \"nodemon dev-server.js\" \"npm run livereload\"",
    "livereload": "livereload . --wait 500 --debug --extraExts js,css,html",
    "start": "node dev-server.js",
    "watch": "nodemon dev-server.js --watch src --watch assets/styles",
    "docs": "jsdoc -c jsdoc.config.json",
    "docs:serve": "npx http-server docs -p 8081 -o",
    "docs:clean": "rm -rf docs/ && npm run docs",
    "generate-docs": "jsdoc --configure jsdoc.config.json --verbose",
    "staging:prepare": "git checkout -B feature/staging-prepare && node scripts/staging-cleanup.js && git add . && git commit -m 'chore: Prepare frontend for production staging' && git push -f origin feature/staging-prepare && git checkout staging && git reset --hard feature/staging-prepare && git push -f origin staging && echo '🎉 Everything synced!'"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "clean-jsdoc-theme": "^4.3.0",
    "concurrently": "^9.1.2",
    "connect-livereload": "^0.6.1",
    "jsdoc": "^4.0.5",
    "livereload": "^0.9.3",
    "nodemon": "^3.1.10"
  }
}

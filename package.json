{
  "name": "lets-todo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev:api": "pnpm --filter lets-todo-api run dev",
    "dev:app": "pnpm --filter lets-todo-app run dev",
    "format": "pnpm -r run format",
    "format:check": "pnpm -r run format:check"
  },
  "devDependencies": {
    "prettier": "^3.9.6"
  }
}
